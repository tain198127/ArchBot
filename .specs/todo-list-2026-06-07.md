# ArchBot 未完成工作清单

生成日期: 2026-06-07
来源: 全架构审计 (Tauri 2 + Rust 领域模型 + TS/Rust 边界) + Business Flow tasks.md

---

## P0 — BLOCKER（3 项剩余）

| # | 状态 | 来源 | 问题 | 位置 | 预估工作量 |
|---|------|------|------|------|-----------|
| 1 | ✅ 已完成 | Tauri 审计 | CSP null → 配置最小 CSP | `tauri.conf.json` | — |
| 2 | ✅ 已完成 | Tauri 审计 | `db_execute_raw` 移除 Tauri command 注册 | `lib.rs` | — |
| 3 | ✅ 已完成 | 边界审计 | **`conductor.rs` 全部 682 行搬迁到 TypeScript** — DAG 解析、拓扑排序、网关策略、降级链 | `DagEngine.ts` (新) | — |
| 4 | ⬜ 待处理 | Rust 审计 | **21 个 SeaORM Entity 完全未使用** — 决定删除或重构使用 | `db/entities/` | 中 |

---

## P1 — HIGH（7 项）

| # | 状态 | 来源 | 问题 | 位置 | 预估 |
|---|------|------|------|------|------|
| 5 | ⬜ | 边界审计 | `validation.rs` 图校验搬迁到 TypeScript — 循环检测、BFS、Start/End | `FlowGraphValidator.ts` (新) | 中 |
| 6 | ⬜ | Rust 审计 | `Result<String>` 全局 → 定义类型化错误 `DbError` (thiserror) | 全局 | 中 |
| 7 | ⬜ | Rust 审计 | SeaORM Entity + Raw SQL 并存 → 二选一统一 | `db/local_sqlite.rs` + `db/entities/` | 大 |
| 8 | ⬜ | Rust 审计 | 贫血模型 — DB 转换逻辑混在 model.rs，需提取 repository 层 | `business_flow/model.rs` | 小 |
| 9 | ⬜ | Tauri 审计 | 6 处 `OnceLock<Mutex<T>>` → 迁移到 `tauri::State<T>` + `app.manage()` | `db/`, `fs/`, `vector/`, `license.rs` | 中 |
| 10 | ⬜ | Tauri 审计 | 硬件指纹通过 shell 命令 → 使用 `mac_address` crate | `license.rs:73-121` | 小 |
| 11 | ⬜ | Tauri 审计 | HTTP API (Axum) 无认证 → 添加 Bearer token | `server.rs` | 中 |

---

## P2 — MEDIUM（18 项）

### 边界搬迁

| # | 状态 | 来源 | 问题 | 位置 | 预估 |
|---|------|------|------|------|------|
| 12 | ⬜ | 边界审计 | handler 业务规则提取 — published/builtin 不可变性、"(Copy)" 命名 | `handler.rs:139-176,239` | 小 |
| 13 | ⬜ | 边界审计 | `ai_config.rs` 110 行硬编码 9 个 Provider 定义 → `aiProviders.ts` | `ai_config.rs:35-145` | 小 |
| 14 | ⬜ | 边界审计 | session state machine 提取 → TS | `session_manager.rs:314-326` | 小 |
| 15 | ⬜ | 边界审计 | context_assembly 滑动窗口/执行策略 → 参数由 TS 传入 | `context_assembly.rs:78-181` | 小 |

### Tauri 合规

| # | 状态 | 来源 | 问题 | 位置 | 预估 |
|---|------|------|------|------|------|
| 16 | ⬜ | Tauri 审计 | 缺少 `src-tauri/capabilities/` — Tauri 2 permissions 模型未启用 | 新建目录 | 小 |
| 17 | ⬜ | Tauri 审计 | `withGlobalTauri: true` — 第三方脚本可访问 Tauri API | `tauri.conf.json` | 小 |
| 18 | ⬜ | Tauri 审计 | `read_local_file` 可读任意文件 → 限制到项目目录 | `fs/mod.rs` | 小 |
| 19 | ⬜ | Tauri 审计 | `load_http_config` 静默吞没错误 → 至少加 warn 日志 | `lib.rs:31-44` | 极小 |
| 20 | ⬜ | Tauri 审计 | 部分 struct 缺少 `rename_all = "camelCase"` | 各 handler | 极小 |

### Rust 代码质量

| # | 状态 | 来源 | 问题 | 位置 | 预估 |
|---|------|------|------|------|------|
| 21 | ⬜ | Rust 审计 | String 代替 enum — flow_type, status, severity, operator | `model.rs` 等 | 中 |
| 22 | ⬜ | Rust 审计 | 死文件清理 — `local_sqllite.rs` (空), `lancedb_store.rs` (废弃) | 删除 | 极小 |
| 23 | ⬜ | Rust 审计 | `conductor.rs:147` unwrap() → `ok_or_else` | 1行 | 极小 |
| 24 | ⬜ | Rust 审计 | bool 解析在三处重复 → 提取共享函数 | `model.rs`, `handler.rs`, `digital_employee.rs` | 小 |
| 25 | ⬜ | Rust 审计 | `lancedb_store.rs` 使用 std Mutex (潜在死锁) → 删除文件 | 删除 | 极小 |
| 26 | ⬜ | Rust 审计 | 测试覆盖严重不足 — handler/digital_employee/secret 无测试 | 多文件 | 大 |
| 27 | ⬜ | Rust 审计 | 中英文混用 → 统一为英文 | 全局 | 中 |

### 前端/配置

| # | 状态 | 来源 | 问题 | 位置 | 预估 |
|---|------|------|------|------|------|
| 28 | ⬜ | Rust 审计 | 缺少 `ts-rs` 自动生成类型 → 手动同步易遗漏 | `types/` vs Rust struct | 小 |
| 29 | ⬜ | 边界审计 | `data_standard.rs` 默认约定硬编码 → TS config | `data_standard.rs:158-170` | 小 |

---

## P3 — LOW / 优化（8 项）

| # | 状态 | 来源 | 问题 | 位置 | 预估 |
|---|------|------|------|------|------|
| 30 | ⬜ | Tauri 审计 | `handlers/` 重命名为 `http/` | 目录 | 小 |
| 31 | ⬜ | Tauri 审计 | HTTP Server 默认启用 → 添加 feature flag | `Cargo.toml` | 小 |
| 32 | ⬜ | Tauri 审计 | 重复 `now_iso()` → 删除 `fs/mod.rs` 中的副本 | `fs/mod.rs` | 极小 |
| 33 | ⬜ | Rust 审计 | 缺少 newtype — FlowId, RunId, ArtifactId | 类型系统 | 中 |
| 34 | ⬜ | 边界审计 | `turn_executor.rs` 硬编码中文 prompt 模板 → i18n config | `turn_executor.rs:590-777` | 小 |
| 35 | ⬜ | 边界审计 | `shell_control.rs` 默认黑名单 → 合理保留，加配置覆盖能力 | `shell_control.rs:7-45` | 小 |
| 36 | ⬜ | 边界审计 | `agent_config_handler.rs` URL 转换与 turn_executor 重复 → 去重 | 两文件 | 小 |
| 37 | ⬜ | 边界审计 | `scenario.rs`, `context.rs` ✅ 无违规 (纯 I/O) | — | — |

---

## 原 tasks.md 未完成项（不属于审计范围）

| # | 状态 | 任务 | 所属分组 |
|---|------|------|---------|
| 38 | ⬜ | Conductor 执行引擎 — streaming events、abort 注册表、sub-flow 递归 | Group 8 (P5) |
| 39 | ⬜ | Run Panel 前端 — 实时事件流、节点输出、abort 按钮 | Group 9 |
| 40 | ⬜ | AI Features — AI 验证、NL→BPMN、语义结果展示 | Group 10 |
| 41 | ⬜ | E2E 测试 — 创建/编辑/运行流程端到端 | Group 12.6 |
| 42 | ⬜ | Capabilities 权限文件创建 | 新增（Tauri 审计发现） |

---

## 统计

| 优先级 | 总数 | 已完成 | 待处理 |
|--------|------|--------|--------|
| P0 BLOCKER | 4 | 2 | 2 |
| P1 HIGH | 7 | 0 | 7 |
| P2 MEDIUM | 18 | 0 | 18 |
| P3 LOW | 8 | 0 | 8 |
| 未完成功能 | 5 | 0 | 5 |
| **合计** | **42** | **2** | **40** |

---

## 建议执行批次

**第一批（本周）**: #3, #4 — 两个 BLOCKER
**第二批（下周）**: #5, #6, #7, #8, #9, #10, #11 — 7 个 HIGH
**第三批（本月）**: #12~#29 — 18 个 MEDIUM
**第四批（下月）**: #30~#37 — 8 个 LOW
**第五批（后续）**: #38~#42 — 功能实现
