## 1. Rust 依赖

- [x] 1.1 在 `src-tauri/Cargo.toml` 中添加 `lancedb` 依赖

## 2. LanceDB 模块

- [x] 2.1 新建 `src-tauri/src/lancedb_store.rs` —— 全局连接管理 + 表操作 + 搜索
- [x] 2.2 在 `src-tauri/src/lib.rs` 中声明模块并注册 Tauri commands

## 3. PRD

- [x] 3.1 在 `prd.yml` 中记录 LanceDB 集成
