## 1. 配置层

- [x] 1.1 在 `src/config/projectDirs.ts` 中插入 `defect` 条目（chaosTest 之后、deployment 之前），颜色使用 `#d35400`

## 2. i18n

- [x] 2.1 在 `src/i18n/zh-CN.ts` 的 `projectDirs` 中添加 `defect: '缺陷'`
- [x] 2.2 在 `src/i18n/en-US.ts` 的 `projectDirs` 中添加 `defect: 'Defect'`

## 3. 文档同步

- [x] 3.1 更新 `prd.yml` 中项目子目录列表，添加"缺陷"（放在部署之前）
